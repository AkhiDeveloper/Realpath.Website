@page
@model Realpath.NewWebSite.Pages.Contactus.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>CompanyInfo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CompanyInfo.id" class="control-label"></label>
                <input asp-for="CompanyInfo.id" class="form-control" />
                <span asp-validation-for="CompanyInfo.id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CompanyInfo.name" class="control-label"></label>
                <input asp-for="CompanyInfo.name" class="form-control" />
                <span asp-validation-for="CompanyInfo.name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CompanyInfo.logoUrl" class="control-label"></label>
                <input asp-for="CompanyInfo.logoUrl" class="form-control" />
                <span asp-validation-for="CompanyInfo.logoUrl" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
